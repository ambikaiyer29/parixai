-- Dev seed data (run after schema migration)
-- Creates a test org, project, prompt, test set, and experiment

-- Note: Insert a user via Supabase Auth UI first, then run:
-- UPDATE seed with your actual user ID
-- This seed is for reference only; actual dev data created via UI

SELECT 'Seed file ready. Create users via Supabase Auth, then add org_members manually.' AS message;
